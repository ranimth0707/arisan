# rust / anchor build output, including the program keypair
target
.anchor
**/*.rs.bk

# js
node_modules
dist
.vercel

# never commit keys or env files
*.env
.env*
*keypair*.json
*.pem

.DS_Store
test-ledger
.yarn
.surfpool

# Local tooling scratch. Never published: the repo is the project, not the
# machinery that happened to build it.
.claude/
.superstack/
